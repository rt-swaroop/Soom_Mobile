const BASE_URL = 'https://office-app-api.remote-teams.in/api/soom'

const API_ROUTES = {
    AUTH: `${BASE_URL}/auth`,
    ATTENDANCE: `${BASE_URL}/attendance`,
    LEAVES: `${BASE_URL}/leaves`,
    TIMEOFF: `${BASE_URL}/timeoff`,
};

export default API_ROUTES;
